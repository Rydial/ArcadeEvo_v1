#version 460

///////////////////////////////////// UBOs /////////////////////////////////////

// ...

//////////////////////////////////// Inputs ////////////////////////////////////

layout(location = 0) in      vec2 vTexCoord;
layout(location = 1) flat in uint vTexIndex;

/////////////////////////////////// Outputs ////////////////////////////////////

layout(location = 0) out vec4 fragColor;

////////////////////////////////////////////////////////////////////////////////

void main()
{
  // vec4 fragColor = texture(sampler2D(textures[vTexIndex], texSampler), vTexCoord);

  // Set Fragment Color
  // fragColor = fragColor;

  fragColor = vec4(1.0f, 1.0f, 0.0f, 1.0f);
}